#' Werner's colours palettes
#' @rdname WernerPals
#' @format ## `WernerPals`
#' An object of class `palettes_palette` with
#' `r length(WernerPals)` discrete colour palettes.
#'  Use `names(WernerPals)` to return all palette names.
"WernerPals"
